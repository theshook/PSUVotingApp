<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="psuva.com.ph.psuvotingsystem">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PartyListAdd" />
        <activity android:name=".PartyListUpdate" />
        <activity android:name=".VoterAdd" />
        <activity android:name=".VoterUpdate" />
        <activity android:name=".BusinessManagerActivity" />
        <activity android:name=".PresidentActivity" />
        <activity android:name=".ProjectAndActivitiesActivity" />
        <activity android:name=".ExecutiveVicePresidentActivity" />
        <activity android:name=".CommunicationAcitivity" />
        <activity android:name=".StudentRightsActivity" />
        <activity android:name=".AuditorActivity" />
        <activity android:name=".PublicRelationsOfficeActivity"></activity>
    </application>

</manifest>